{
  "name": "@devflow/tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "happy-dom": "^14.0.0",
    "@testing-library/jest-dom": "^6.4.0"
  },
  "dependencies": {
    "@devflow/shared": "*"
  }
}
